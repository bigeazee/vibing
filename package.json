{
  "name": "vibing",
  "version": "0.1.0",
  "private": true,
  "description": "A top-down browser game about AI-assisted development. No build step, no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
